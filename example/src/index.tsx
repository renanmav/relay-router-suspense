import React from 'react'
import ReactDOM from 'react-dom'
// import { createRouter } from 'relay-router-suspense'

import App from './App'

// const router = createRouter()

ReactDOM.createRoot(document.getElementById('root')!).render(<App />)
